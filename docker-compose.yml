services:
  server:
    build: ./image
    restart: unless-stopped
    volumes:
      - ./data/server:/data/server:ro
      - ./data/minecraft:/data/minecraft
    ports:
      - ${MINECRAFT_GAME_PORT}:25565/tcp
      - ${MINECRAFT_GAME_PORT}:25565/udp
      - ${MINECRAFT_DYNMAP_PORT}:8123/tcp